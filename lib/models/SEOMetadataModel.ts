export type SeoMetadataModel = {
    title: string,
    description: string,
    ogTitle: string,
    ogDescription: string,
    ogImage: null,
    hidePage: boolean,
    excludeLinks: boolean,
    __typename: string
}